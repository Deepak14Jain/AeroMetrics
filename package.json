{
  "name": "AeroMetrics-cds",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "type": "module",
  "sapux": [
    "app/airports"
  ],
  "devDependencies": {
    "@sap/ux-specification": "UI5-1.134",
    "grunt": "^1.6.1",
    "grunt-cli": "^1.5.0",
    "grunt-karma": "^4.0.2",
    "json2csv": "^6.0.0-alpha.2",
    "karma": "^6.4.4",
    "karma-chrome-launcher": "^3.2.0",
    "karma-coverage": "^2.2.1",
    "karma-html-reporter": "^0.2.7",
    "karma-junit-reporter": "^2.0.1",
    "karma-qunit": "^4.2.1",
    "karma-ui5": "^4.1.0",
    "chalk": "^5.2.0"
  },
  "scripts": {
    "prepare-server": "npm install && cd app/airports && npm install && cd ../.. && mvn clean install",
    "mockserver": "npm run scripts && npm run mockserver-run",
    "mockserver-run": "cd ./srv && mvn clean spring-boot:run -U -Dspring-boot.run.jvmArguments=\"-Dspring.profiles.active=default\"",
    "scripts": "cd scripts && node airportsWithoutIata.js && node commonTimeZones.js && node averageElevation.js",
    "test-server": "cd app/airports && npm run test-server",
    "opa-tests": "cd app/airports && npm run test-opa"
  }
}
